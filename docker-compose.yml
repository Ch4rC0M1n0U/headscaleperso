###############################################################################
# HEADSCALE + HEADPLANE - Configuration Docker Compose
# Port 8443 - Camouflé et durci
###############################################################################

services:
  #############################################################################
  # HEADSCALE - Serveur de coordination Tailscale self-hosted
  #############################################################################
  headscale:
    image: headscale/headscale:0.27.1
    container_name: headscale
    restart: unless-stopped
    command: serve
    labels:
      me.tale.headplane.target: headscale
    volumes:
      - ./headscale/config/config.yaml:/etc/headscale/config.yaml:ro
      - ./headscale/config/dns_records.json:/etc/headscale/dns_records.json
      - ./headscale/data:/var/lib/headscale
    networks:
      - headscale-net
    healthcheck:
      test: ["CMD", "headscale", "health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  #############################################################################
  # HEADPLANE - Interface Web pour Headscale
  #############################################################################
  headplane:
    image: ghcr.io/tale/headplane:latest
    container_name: headplane
    restart: unless-stopped
    depends_on:
      headscale:
        condition: service_healthy
    volumes:
      - ./headplane/config/config.yaml:/etc/headplane/config.yaml:ro
      - ./headplane/data:/var/lib/headplane
      - ./headscale/config/config.yaml:/etc/headscale/config.yaml
      - ./headscale/config/dns_records.json:/etc/headscale/dns_records.json
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - headscale-net
    environment:
      - TZ=Europe/Brussels

  #############################################################################
  # CADDY - Reverse Proxy HTTPS sur port 8443 (camouflé)
  #############################################################################
  caddy:
    image: caddy:2-alpine
    container_name: headscale-caddy
    restart: unless-stopped
    ports:
      - "8443:8443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./caddy/data:/data
      - ./caddy/config:/config
    networks:
      - headscale-net
    environment:
      - TZ=Europe/Brussels

networks:
  headscale-net:
    driver: bridge
